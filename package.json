{
  "name": "solana-dapp",
  "version": "2.0.0",
  "description": "Create GIF collection dapp",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "contract": "yarn workspace contract",
    "client": "yarn workspace client",
    "client:dev": "yarn workspace client dev",
    "test": "yarn workspace contract test",
    "anchor:deploy": "cd packages/contract && anchor deploy && cd ..",
    "anchor:build": "cd packages/contract && anchor build && cd .."
  },
  "devDependencies": {}
}
